# Frontend env - set to your Render backend URL to enable backend mode
# If empty, app uses local in-memory engine (no backend needed, works offline)
VITE_API_URL=https://roumi-backend.onrender.com

# For local backend dev
# VITE_API_URL=http://localhost:3001
